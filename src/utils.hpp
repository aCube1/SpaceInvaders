#ifndef _UTILS_HPP_
#define _UTILS_HPP_

#include <bitset>
#include <cstdint>

namespace game {
	inline constexpr auto SCREEN_WIDTH { 256 };
	inline constexpr auto SCREEN_HEIGHT { 224 };
} // namespace game

#endif /* _UTILS_HPP_ */
